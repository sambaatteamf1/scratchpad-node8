{
  "name": "algotest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.5.2",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-register": "^6.24.1",
    "graphql": "^0.10.3",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "babel-polyfill": "^6.23.0",
    "babel-preset-stage-0": "^6.24.1",
    "babel-watch": "^2.0.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node bin/dev",
    "build": "mkdir -p dist && babel epi -s -d dist",
    "watch": "mkdir -p dist && babel epi -s -d dist -w",
    "prod": "npm run build && node bin/prod"
  },
  "author": "",
  "license": "ISC"
}
